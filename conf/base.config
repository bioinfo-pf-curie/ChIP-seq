params {

  // notifications
  email = false

  // cluster
  queue = null

  // genome annotations
  genomeAnnotationPath = ""
  
  // output directories
  outputDir = './results'
  summaryDir = "${params.outputDir}/summary"

  // conda
  condaCacheDir = "/tmp/conda-cache-nextflow"

  containers {
    // singularity
    singularityImagePath = ""
    singularityRunOptions =  ""

    // path profile
    profilePath = "/data/users/nservant/projects_analysis/kdi_home/conda/chipseq/bin"
  }
}